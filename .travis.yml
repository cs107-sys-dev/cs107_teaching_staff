# Configuration file for TravisCI builds
language: python
python:
    - "3.6"

# This project is configured with PEP517 via setuptools
install:
    (cd python_project && python -m pip install --use-feature=in-tree-build .)

# Run the tests
script:
    - (cd python_project/tests && python -m unittest discover)
